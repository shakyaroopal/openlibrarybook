package org.example.openlibrarybook.Mapper;

import org.example.openlibrarybook.input.BookDto;
import org.example.openlibrarybook.output.Book;


public class BookMapper {
//    public static Book convertToBookEntity(BookDto bookDto) {
//        return new Book(bookDto.getId(), bookDto.getIsbn(), bookDto.getTitle(), bookDto.getPublishDate(), bookDto.getNumberOfPages(), bookDto.getUrl());
//    }
//    public static BookDto convertToBookDto(Book book) {
//        return new BookDto(book.getId(), book.getIsbn(), book.getTitle(), book.getPublishDate(), book.getNumberOfPages(), book.getUrl());
//    }
}
